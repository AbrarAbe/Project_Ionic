import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marbel.one',
  appName: 'Marbel One',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
